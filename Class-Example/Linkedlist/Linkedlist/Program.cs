using System;
using Linkedlist.Node;

namespace Linkedlist
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }

        // Create a node class that has the properties of value and next.
            // Test: Make sure that the node class has a value property;
            // make sure the node class has a next property
        
        // within your LL class, include head propety
            // Test: Heade is null upon instantiation

        // define a method called insert which takes any value as an arg and add new node with that value to the head of the linklist. 0(1) operation
    
    }
}
